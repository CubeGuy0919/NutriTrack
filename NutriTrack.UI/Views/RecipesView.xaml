<UserControl x:Class="NutriTrack.UI.Views.RecipesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📖 Recipes Database" FontSize="26" FontWeight="Bold"
                       Foreground="{StaticResource PrimaryDarkBrush}" Margin="0,0,0,4" />
            <TextBlock Text="Browse your recipes categorized by cuisine and food type."
                       Foreground="#6B7A70" FontSize="13" />
        </StackPanel>

        <DataGrid x:Name="RecipesGrid" Grid.Row="1"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  Background="White"
                  BorderBrush="#E3E8E4"
                  BorderThickness="1" />
    </Grid>
</UserControl>